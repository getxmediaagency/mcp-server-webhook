# Core dependencies for the Modular MCP Server
aiohttp>=3.8.0
asyncio-mqtt>=0.11.0
python-dotenv>=0.19.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
flake8>=4.0.0

# Optional dependencies for enhanced features
uvloop>=0.17.0  # Faster event loop for Linux/macOS
orjson>=3.8.0   # Faster JSON processing
